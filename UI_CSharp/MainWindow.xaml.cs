using System;

namespace CasperManager
{
    // Console project placeholder to avoid broken/empty compile unit.
    internal static class MainWindowPlaceholder
    {
        internal static void Initialize()
        {
            Console.WriteLine("[UI_CSharp] MainWindow placeholder initialized.");
        }
    }
}
